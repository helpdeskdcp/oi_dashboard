# Dev-only tooling for agents/dev_agent/gates/code_quality.py (Gate 5:
# lint, type check, security scan, dependency vulnerability scan).
# Not required to run the app itself -- see requirements.txt for that.
ruff>=0.16.0
mypy>=2.3.0
bandit>=1.9.0
pip-audit>=2.10.0
